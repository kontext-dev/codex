version: "1"
label: "upstream-sync"
conflictLabel: "merge-conflict"
rules:
  - base: main
    upstream: openai:main
    mergeMethod: hardreset
    mergeUnstable: true

  - base: kontext-dev
    upstream: main
    mergeMethod: merge
    mergeUnstable: false
    assignees:
      - michiosw
    conflictReviewers:
      - michiosw
